# Server Configuration
PORT=3002 # Port for the server to listen on
NODE_ENV=development # Set to production for production mode or development for development mode
ENVIRONMENT=DEV  # Set to PROD for production mode or DEV for development mode
DEBUG=true       # Enable debug logging

# Database Configuration - MySQL
DB_CONNECTION_STRING=mysql://username:password@localhost:3306/dbname # Full connection string (preferred method)
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=dbname
# DB_USER=username
# DB_PASSWORD=password
# DB_DIALECT=mysql

# Session Secret - Use one of the methods below to generate a secure key:
# 1. Node.js command: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 2. OpenSSL: openssl rand -hex 64
# 3. Online generators (less secure): https://passwordsgenerator.net/
SESSION_SECRET=your-secret-key-here
